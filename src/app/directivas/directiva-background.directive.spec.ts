import { DirectivaItemClassDirective } from './directiva-background.directive';

describe('DirectivaItemClassDirective', () => {
  it('should create an instance', () => {
    const directive = new DirectivaItemClassDirective();
    expect(directive).toBeTruthy();
  });
});
